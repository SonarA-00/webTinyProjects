git init  //to let git keep on track of our project
git add . //add all files in that project(folder) to the stage prepare for save it to Local repository
git commit -m "First commit"  //save files to Local repository
git remote add origin https://github.com/yourname/repo.git    // add  this origin  url to git remote repository to tell git where we want to ass files to(url)
git push -u origin main // push files into that origin main

** their are two repository 
1 Lcoal repository (in our computer)
2 remote repository (in git/ github/...)